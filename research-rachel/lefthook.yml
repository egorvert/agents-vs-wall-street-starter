pre-commit:
  parallel: true
  commands:
    format-check:
      run: npm run format:check
    lint:
      run: npm run lint
    react-doctor:
      run: npm run doctor:staged
    tests:
      run: npm test

pre-push:
  commands:
    full-check:
      run: npm run check
